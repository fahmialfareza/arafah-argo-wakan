<IfModule mod_rewrite.c>
  RewriteEngine On

# If the requested filename is not a directory or file, rewrite to index.html

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [QSA,L]
</IfModule>
